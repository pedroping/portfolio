/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SelectLastFolderDirective } from './select-last-folder.directive';

describe('Directive: SelectLastFolder', () => {
  it('should create an instance', () => {
    const directive = new SelectLastFolderDirective();
    expect(directive).toBeTruthy();
  });
});
