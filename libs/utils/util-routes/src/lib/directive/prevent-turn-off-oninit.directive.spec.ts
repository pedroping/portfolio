/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PreventTurnOffOninitDirective } from './prevent-turn-off-oninit.directive';

describe('Directive: PreventTurnOffOninit', () => {
  it('should create an instance', () => {
    const directive = new PreventTurnOffOninitDirective();
    expect(directive).toBeTruthy();
  });
});
