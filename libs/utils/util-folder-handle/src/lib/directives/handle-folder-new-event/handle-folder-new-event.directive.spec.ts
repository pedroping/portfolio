/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HandleFolderNewEventDirective } from './handle-folder-new-event.directive';

describe('Directive: HandleFolderNewEvent', () => {
  it('should create an instance', () => {
    const directive = new HandleFolderNewEventDirective();
    expect(directive).toBeTruthy();
  });
});
