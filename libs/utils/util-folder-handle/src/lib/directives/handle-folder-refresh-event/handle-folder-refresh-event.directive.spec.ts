/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HandleFolderRefreshEventDirective } from './handle-folder-refresh-event.directive';

describe('Directive: HandleFolderRefreshEvent', () => {
  it('should create an instance', () => {
    const directive = new HandleFolderRefreshEventDirective();
    expect(directive).toBeTruthy();
  });
});
