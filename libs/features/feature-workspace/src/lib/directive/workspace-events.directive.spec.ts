/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { WorkspaceEventsDirective } from './workspace-events.directive';

describe('Directive: WorkspaceEvents', () => {
  it('should create an instance', () => {
    const directive = new WorkspaceEventsDirective();
    expect(directive).toBeTruthy();
  });
});
