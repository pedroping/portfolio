/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ShowElementPreviewDirective } from './show-element-preview.directive';

describe('Directive: ShowElementPreview', () => {
  it('should create an instance', () => {
    const directive = new ShowElementPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
