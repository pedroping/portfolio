/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BackForwardHandleDirective } from './back-forward-handle.directive';

describe('Directive: BackForwardHandle', () => {
  it('should create an instance', () => {
    const directive = new BackForwardHandleDirective();
    expect(directive).toBeTruthy();
  });
});
