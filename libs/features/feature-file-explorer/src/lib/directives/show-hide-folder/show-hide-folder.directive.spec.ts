/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ShowHideFolderDirective } from './show-hide-folder.directive';

describe('Directive: ShowHideFolder', () => {
  it('should create an instance', () => {
    const directive = new ShowHideFolderDirective();
    expect(directive).toBeTruthy();
  });
});
