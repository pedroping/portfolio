/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OpenSubContextMenuDirective } from './open-sub-context-menu.directive';

describe('Directive: OpenSubContextMenu', () => {
  it('should create an instance', () => {
    const directive = new OpenSubContextMenuDirective();
    expect(directive).toBeTruthy();
  });
});
