/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PreventHandlerDirective } from './prevent-handler.directive';

describe('Directive: PreventHandler', () => {
  it('should create an instance', () => {
    const directive = new PreventHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
