/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PageResizeLeftDirective } from './page-resize-left.directive';

describe('Directive: PageResizeLeft', () => {
  it('should create an instance', () => {
    const directive = new PageResizeLeftDirective();
    expect(directive).toBeTruthy();
  });
});
