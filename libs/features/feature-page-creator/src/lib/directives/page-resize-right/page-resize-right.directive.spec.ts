/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PageResizeRightDirective } from './page-resize-right.directive';

describe('Directive: PageResizeRight', () => {
  it('should create an instance', () => {
    const directive = new PageResizeRightDirective();
    expect(directive).toBeTruthy();
  });
});
