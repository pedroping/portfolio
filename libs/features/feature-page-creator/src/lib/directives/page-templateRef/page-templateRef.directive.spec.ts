/* tslint:disable:no-unused-variable */

import { PageTemplateRefDirective } from './page-templateRef.directive';

describe('Directive: PageTemplateRef', () => {
  it('should create an instance', () => {
    const directive = new PageTemplateRefDirective(
      null as any,
      null as any,
      null as any,
    );
    expect(directive).toBeTruthy();
  });
});
