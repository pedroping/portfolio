/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PageResizeBottomDirective } from './page-resize-bottom.directive';

describe('Directive: PageResizeBottom', () => {
  it('should create an instance', () => {
    const directive = new PageResizeBottomDirective();
    expect(directive).toBeTruthy();
  });
});
