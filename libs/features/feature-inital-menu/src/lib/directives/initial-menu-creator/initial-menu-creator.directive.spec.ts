/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { InitialMenuCreatorDirective } from './initial-menu-creator.directive';

describe('Directive: InitialMenuCreator', () => {
  it('should create an instance', () => {
    const directive = new InitialMenuCreatorDirective();
    expect(directive).toBeTruthy();
  });
});
