/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HandleFolderShortcutDirective } from './handle-folder-shortcut.directive';

describe('Directive: HandleFolderShortcut', () => {
  it('should create an instance', () => {
    const directive = new HandleFolderShortcutDirective();
    expect(directive).toBeTruthy();
  });
});
