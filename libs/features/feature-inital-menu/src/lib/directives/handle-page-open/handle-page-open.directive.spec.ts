/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HandlePageOpenDirective } from './handle-page-open.directive';

describe('Directive: HandlePageOpen', () => {
  it('should create an instance', () => {
    const directive = new HandlePageOpenDirective();
    expect(directive).toBeTruthy();
  });
});
