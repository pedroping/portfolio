/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PreventElementDirective } from './prevent-element.directive';

describe('Directive: PreventElement', () => {
  it('should create an instance', () => {
    const directive = new PreventElementDirective();
    expect(directive).toBeTruthy();
  });
});
