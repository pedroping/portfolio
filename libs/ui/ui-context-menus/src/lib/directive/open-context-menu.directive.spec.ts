/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OpenContextMenuDirective } from './open-context-menu.directive';

describe('Directive: OpenContextMenu', () => {
  it('should create an instance', () => {
    const directive = new OpenContextMenuDirective();
    expect(directive).toBeTruthy();
  });
});
